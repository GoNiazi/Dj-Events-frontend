import React from "react";
import Layout from "@/components/Layout";
const Events = () => {
  return (
    <Layout title="All the new events in the city">
      <h1>Events</h1>
    </Layout>
  );
};

export default Events;
